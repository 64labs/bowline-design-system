---
title: Components
---

import { Link } from "gatsby"

# Components

Get to know Bowline's React components.

- Control nearly every aspect with a well-defined theme configuration
- Map multiple design tokens to your theme's breakpoints with responsive props
- Straightforward layouts with Stack, Inline, and Columns
- Auto-detected contrast ratio adapts text and other component styles to the current background color

<Box />

#### Primitives

<Inline space="small" marginBottom="gutter">
  {['Box','Stack'].map(item => (
    <Box
      as={Link}
      to={`/components/${item.toLowerCase()}/`}
      display="block"
      boxShadow="borderStandard"
      paddingY="gutter"
      paddingX="large"
      borderRadius="standard"
    >
      <Text weight="medium" tone="brand">
        {item}
      </Text>
    </Box>
  ))}
</Inline>

#### Layout

<Inline space="small" marginBottom="gutter">
  {['Stack',
    'Inline',
    'Columns',
    'Column',
    'ContentBlock',
    'ScrollBox',
    'Divider',
    'VerticalDivider'].map(item => (
      <Box
        as={Link}
        to={`/components/${item.toLowerCase()}/`}
        display="block"
        boxShadow="borderStandard"
        paddingY="gutter"
        paddingX="large"
        borderRadius="standard"
      >
        <Text weight="medium" tone="brand">
          {item}
        </Text>
      </Box>
    ))}
</Inline>

#### Interactive

<Inline space="small" marginBottom="gutter">
  {['Button', 'Checkbox', 'Input', 'Radio', 'Select', 'Textarea'].map(item => (
    <Box
      as={Link}
      to={`/components/${item.toLowerCase()}/`}
      display="block"
      boxShadow="borderStandard"
      paddingY="gutter"
      paddingX="large"
      borderRadius="standard"
    >
      <Text weight="medium" tone="brand">
        {item}
      </Text>
    </Box>))}
</Inline>

#### Display

<Inline space="small" marginBottom="gutter">
  {['Icon'].map(item => (
    <Box
      as={Link}
      to={`/components/${item.toLowerCase()}/`}
      display="block"
      boxShadow="borderStandard"
      paddingY="gutter"
      paddingX="large"
      borderRadius="standard"
    >
      <Text weight="medium" tone="brand">
        {item}
      </Text>
    </Box>))}
</Inline>