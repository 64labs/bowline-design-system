import {ContentBlock} from '@64labs/bowline-design-system'

export const meta = {
  name: 'ContentBlock',
  component: ContentBlock,
  description: 'A container with max-width and padding built-in.',
  inheritBoxProps: true,
  playroom: '',
}
